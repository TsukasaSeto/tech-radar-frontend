# Routine Feedback 2026-05-28

## 実行サマリ
- 採用: 1件 / 強化: 2件
- 採用前チェックでスキップ: 0件
- 取得失敗ソース: 0件（全段階スキップ: Turborepo Blog 1件）
- 気づきレベル: 軽微

## 観察した問題（2件）

### 1. _seen.json の旧フォーマット（URLのフラットリスト）との乖離
- **種類**: プロンプト記述
- **発生箇所**: Step 6（既読除外処理）
- **詳細**: プロンプトの `_seen.json` 仕様スキーマは `{ version, updated_at, entries: [{url, fetched_at, status}] }` の構造を前提にしているが、実際のリポジトリの `_seen.json` はURLの平文フラットリスト（`["https://...", ...]`）形式のまま。チェック処理自体は機能するが、毎回プロンプトのスキーマ記述とずれており混乱の原因になる。
- **影響**: 処理ロジックへの影響なし（URLの存在確認は両形式で等価）
- **再現性**: 継続的（過去のfeedbackでも指摘があるかもしれない）

### 2. Zenn performance タグの記事数が少ない
- **種類**: ソース品質
- **発生箇所**: Step 4（Zenn performance フィード）
- **詳細**: 今回の36h窓では1件のみ（React 19 Compiler記事だが窓外境界付近）。performance タグは新着記事が少なく、採用できる記事が長期間ゼロが続いている可能性がある。
- **影響**: パフォーマンス関連の新規ルール採用機会が低い
- **再現性**: 継続的（前回の2026-05-25 feedbackでも確認）

## プロンプト改善提案（1件）

### 1. _seen.json スキーマのドキュメントを実態に合わせて修正する
- **対象セクション**: `## _seen.json の仕様 > ### スキーマ`
- **現状の記述**:
  ```json
  {
    "version": "1.0",
    "updated_at": "...",
    "entries": [{ "url": "...", "fetched_at": "...", "status": "adopted" }]
  }
  ```
- **提案する変更**: 実際のフォーマット（URLのフラットリスト `["https://..."]`）に合わせるか、または「現在はフラットリスト形式だが将来的にはオブジェクト形式に移行予定」という注記を追加する
- **理由**: プロンプトのスキーマと実実装が乖離していると、将来のRoutineが誤った更新処理を実装するリスクがある
- **想定リスク**: スキーマ変更に合わせた `_seen.json` フォーマット変換が必要になる
- **優先度**: 中

## 取り上げなかったが気になる挙動

- `actions/checkout` の `persist-credentials: false` を `sisakulint` で検出する記事（`zenn.dev/ultrasupara`）が今回の窓内に出現した。先週の Rule #8 の根拠として使えたかもしれない。逆に言えば、今週追加した Rule #8 の直後に「検出ツール」の記事が出るという面白いタイミングの一致。
- dev.to testing タグで Playwright の記事（"Playwright is a test framework, not an automation tool"）が出ていた。testing.md の強化候補になりうるが、dev.to の単一記事のため今回はD判定。
