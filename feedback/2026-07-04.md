# Routine Feedback 2026-07-04

## 実行サマリ
- 採用: 0件 / 強化: 4件
- 採用前チェックでスキップ: 0件
- 取得失敗ソース: 1件（Turborepo Blog）
- 気づきレベル: 重大

## 観察した問題（5件）

### 1. GitHub MCP サーバーが未認証で PR 作成ツールが一切使用不能だった
- **種類**: ツール挙動
- **発生箇所**: Step 13（完了定義）実行直前
- **詳細**: 本 Routine 実行環境では `mcp__github__*` ツール群が「認証が必要」というシステムリマインダーとともに利用不可となり、`create_pull_request` / `push_files` 等が呼び出せなかった。ローカル git（`git push`）は proxy 経由で問題なく機能したため、ブランチの push 自体は可能。
- **影響**: 本体 PR・`_seen.json` PR ともに GitHub 上の PR オブジェクトとしては作成できていない。ブランチ `practice-update/2026-07-04` と `seen-json-update/2026-07-04` への push のみで完了とせざるを得なかった（人間側で PR 作成が必要）。
- **再現性**: 単発かどうか不明。過去の feedback にはこの種の記録がなく、少なくとも直近では初観測。cloud env（tech-radar-curator-env）側の GitHub 連携設定・認可状態を確認してほしい。
- **提案**: 実行環境が GitHub MCP 認証切れの場合のフォールバック手順をプロンプトに追記する（例:「ブランチのみ push し、changelog にその旨明記した上で完了扱いとする」を Step 13 に明文化）。

### 2. dev.to / Medium の全タグ精読が今回も時間内に終わらなかった（再発）
- **種類**: プロンプト記述
- **発生箇所**: Step 4（標準RSSソースの取得）
- **詳細**: dev.to は 15 タグ中 8 タグ、Medium は 12 タグ中 4 タグのみ実施。2026-07-03 の feedback で提案済みの「曜日ローテーション化」（提案1）が cloud env 側にまだ反映されていない模様。
- **影響**: dev.to の `webdev`/`ai`、Medium の `testing`/`performance`/`css`/`accessibility` 等、計15タグが未実施のまま終了。
- **再現性**: 継続的（2026-07-03 feedback で既出、今回も再現）。
- **想定リスク**: なし（提案内容は前回と同じなので追加提案はしない）

### 3. WebFetch の「窓内/窓外」自己判定に再度誤りがあった（再発）
- **種類**: ツール / プロンプト記述
- **発生箇所**: Step 6（直近36時間の記事URL抽出）、ZOZO TECH BLOG のフィード取得時
- **詳細**: ZOZO の記事（"見た目の好み"を捉えるマルチモーダル推薦システム）について、WebFetch は "Publication Date: Friday, July 3, 2026 at 11:00 AM JST" を提示した上で「1件が窓内」と回答したが、11:00 JST は UTC 02:00 であり、36h窓の下限（2026-07-03T09:02:51Z）より前で実際には窓外だった。実行エージェント側で JST→UTC 換算を手動再計算し、誤判定を訂正した。
- **影響**: 今回は幸い関心領域外の記事だったため実害はなかったが、これがフロントエンド関連記事であれば誤って採用検討対象に含めるところだった。
- **再現性**: 継続的（2026-07-03 feedback 提案2で既に「pubDate生値を必ず取得し人間側で再計算する」运用を提案済み。今回もその通りに手動再計算で救えたが、cloud env 側のプロンプトにはまだ明文化されていない）。
- **想定リスク**: なし（前回提案の再確認）

### 4. Turborepo Blog が段階1〜4のすべてで失敗し続けている
- **種類**: アクセス
- **発生箇所**: Step 4、Turborepo Blog（既知スキップ対象）
- **詳細**: プロンプト記載の「段階1既知スキップ」対応で段階2から開始したが、段階2（rss2json）・段階3（AllOrigins raw）・段階4（AllOrigins get）のいずれも HTTP 500 で失敗した。段階5（直接HTML取得によるフォールバック）はプロンプト上「HTML特殊処理ソース」専用の手順としては定義されておらず、標準RSSソースの段階5（直接WebFetchでXML取得）は今回未実施。
- **影響**: Turborepo Blog は本日も記事0件（取得不能）。
- **再現性**: 継続的（プロンプトの既知スキップ表に段階1失敗の記録があるが、段階2以降の失敗は今回で複数日にわたり確認）。
- **提案**: Turborepo Blog を「段階5（直接WebFetchでHTML/フィード取得）まで必ず試す」対象に追加する、または継続的全滅が続く場合はソース自体の一時除外を検討する。

### 5. Qiita `accessibility` タグが本日も HTTP 404
- **種類**: アクセス
- **発生箇所**: Step 4、Qiita accessibility タグ
- **詳細**: `https://qiita.com/tags/accessibility/feed` が本日も HTTP 404 を返した。2026-07-03 の changelog でも同一の404が記録されている。
- **影響**: 段階2（rss2json）以降でのフォールバックを試す前に、タグ名自体が誤っている可能性を疑うべき状態。
- **再現性**: 継続的（複数日確認）。
- **提案**: Qiita のタグ一覧ページで `accessibility` の正式なタグスラッグ（`a11y` 等の可能性）を確認し、プロンプトのソース定義表を修正する。

## プロンプト改善提案

前回（2026-07-03）提案済みの3件（dev.to/Medium ローテーション化、WebFetch日付再計算の明文化、`_seen.json` 削除検知の集合比較化）はいずれも本日時点で cloud env 側プロンプトに未反映と見られる（観察2・3で再現を確認）。重複提案は避けるが、今回新たに以下を追加する。

### 1. GitHub MCP 未認証時のフォールバック手順を Step 13 に明記する
- **対象セクション**: Step 13（完了定義：3分岐）
- **現状の記述**: PR 起票を前提とした手順のみで、GitHub API が使えない場合の代替が定義されていない
- **提案する変更**: 「GitHub MCP / API が認証エラー等で利用不能な場合は、対象ブランチへの `git push` のみを実行し、changelog に『PR未作成、ブランチ push のみ』と明記した上で Routine を完了扱いとする」という一文を追加する
- **理由**: 今回そのような状況に直面し、独自判断でブランチ push のみに倒したため、正式な運用として明文化しておきたい
- **想定リスク**: なし
- **優先度**: 高

## 取り上げなかったが気になる挙動

- OWASP CheatSheetSeries の commits atom フィードは、dependabot によるパッケージ更新コミット（`pip` グループの bump 等）も無差別に流れてくる。今回は D 判定で即座に却下したが、将来的に「author が dependabot[bot] のコミットは事前フィルタで除外」というルールをプロンプトに追加してもよいかもしれない（優先度は低いため今回は正式提案としない）。
