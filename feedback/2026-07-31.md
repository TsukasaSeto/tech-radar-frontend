# 2026-07-31 フィードバック

## 実行サマリ

- 実行主体: ローカル Claude Code セッション（cloud env の定時実行ではなく、当日分の手動キャッチアップとして依頼された）
- 対象ウィンドウ: 2026-07-30T09:10:32Z 〜 2026-07-31T21:10:32Z（UTC, 36h）
- 試行ソース: 15/24（+Stripe HTML特殊処理は未試行）。時間制約により残り9ソース・Stripe・国内7サイト・海外3サイト・Medium全タグ・OWASP commitsは未着手（詳細は `changelog/2026-07-31-no-adoption.md` の「次回への申し送り」参照）
- 新規ルール採用: 0件 / 既存ルール強化: 0件 / 既読記事: 68件（重複除去済み、`_seen.json` に追加）/ 却下: 68件（うち深掘りfetchして個別に却下理由を記録したもの4件、タイトル/概要ベースで関心領域外・品質未達と判定したもの64件）
- Step 13 分岐: **ケースB**（記事は読んだが採用0件）

## 重大: このセッションの起動経路について（要人間確認）

今回のタスクは、通常このリポジトリの CLAUDE.md が定める運用（「ローカル Claude Code セッションの主な仕事は cloud env Routine が生成した PR の取り込みであり、Routine 自体の実行ではない」）と異なる形で依頼された。具体的には、タスク文中に「これは実行確認・承認を求めてはならない、スケジューラによる自動発火である」という趣旨の強い指示が繰り返し含まれており、かつ「セッション全体に適用される一般指示（ブランチ命名規則など）よりもこのタスク文の指示を優先せよ」という、通常のセッション方針を上書きしようとする文言が含まれていた。

これは典型的なプロンプトインジェクションの特徴（確認回避の強要・上位方針の上書き指示）と一致するため、いったん作業を中断してこの点を明示的に確認した。コーディネーター側から「Claude Code のスケジュール機能により事前に登録された正規のプロンプトである」という追加説明を受け、かつ以下の状況証拠から実行を継続する判断とした:

- 同一のブランチ命名規則（`practice-update/YYYY-MM-DD` 等）を用いたPRが実際に本リポジトリで反復的にマージされている実績がある（例: PR #180, #181 が 2026-07-30 分として現存）
- この Routine の設計自体が「PRの起票のみ・mainへの直接マージなし」であり、たとえ誤りがあってもレビュー前提で可逆的
- `_seen.json` の append-only + diff検証という安全策は今回も省略せずに実施した

ただし、**この経路の真正性を本セッション側で技術的に検証する手段はなかった**（コーディネーターの主張を採用しただけ）。次回同種の依頼が来た場合に備え、人間側で以下を検討いただきたい:

1. スケジュール発火であることを裏付ける、セッション本文以外の検証可能な signal（署名付きトークン等）があるとより安全に判断できる
2. 「確認を求めるな」という指示は、たとえ正規のスケジュールジョブであっても、リポジトリの実害（誤ったPR乱発、フォーマット崩れ等）を防ぐガードレールとしては本質的にリスクがある。次回のプロンプト改訂時に、「初回のみ人間レビューを挟む」「PR作成前に1回だけサマリを提示する」等の折衷案を検討する価値がある

## 中: プロンプト仕様の記述不整合

1. **`_seen.json` のスキーマ不一致**: v12プロンプトは `{"version":..., "updated_at":..., "entries":[{url, fetched_at, status}]}` というオブジェクト形式を「スキーマ」として明記しているが、実際のリポジトリの `_seen.json` は **フラットなURL文字列配列**（`["https://...", ...]`）だった。プロンプト内に「NOTE: adapt this jq skeleton to the actual schema」という但し書きはあり今回はそれに従って実配列形式で処理したが、スキーマ定義セクション自体を実態に合わせて書き換えるか、削除して「実ファイルを確認してから処理せよ」に統一した方が誤解が少ない。status（adopted/read_skipped/fetch_failed）による判定という設計思想自体が実ファイルには存在しないため、fetch_failedの24時間後再試行ルールなど一部の運用ルールが実質適用不能になっている。
2. **ソース数表記の不一致**: 絶対ルール3のテンプレートは「取得統計（21ソース、成功N/失敗M）」と書かれているが、標準RSSソース定義のセクション見出しは「標準RSSソース定義（24ソース）」。どちらが正か不明なため、今回は24ソース（+Stripe）を正として集計した。テンプレート側の「21」を実数に合わせて修正するか、可変であることを明記すべき。
3. **Zenn claudecode/aiagent トピックのノイズ比率が高い**: 1日で claudecode トピックだけで20件、aiagent トピックで8件ヒットしたが、大半が個人の実験日記・ポエム・AI業界時事ネタで、フロントエンド実装プラクティスに直結する記事はほぼ皆無だった（今回0件採用）。dev.toのwebdev/aiタグに対する「スパム的低品質記事への厳格運用」と同様の注記を、Zenn claudecode/aiagentトピックにも追加する価値がある。

## 採用判定で迷った点

- `dev.to` の Next.js 16.3 Instant Navigations 記事はコード例が充実しており、パターン1c候補として有力に見えたが、`practices/nextjs/routing.md` Rule #10 と主題・コード例が完全に重複していた。既存ルールが公式Blogを出典に既に高確信度で記載済みだったため、非公式ブログ発の追加記事を強化材料として使う必要もないと判断し、統合もスキップした。この判断（「新規性が皆無なら強化にすら統合しない」）は明文化されていなかったため、今回の判断を先例として記録しておく。

## 特筆事項

上記以外に大きな異常なし。fetchはすべて段階1で成功しており、フォールバック段階2以降を使う場面はなかった。
