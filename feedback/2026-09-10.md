# Routine Feedback 2026-09-10

## 実行サマリ
- 採用: 0件 / 強化: 4件
- 採用前チェックでスキップ: 0件
- 取得失敗ソース: 2件（Stripe Engineering、Qiita `accessibility` タグ）
- 気づきレベル: 中

## 既出問題の再確認（grep結果）

`grep -rl "flat array\|スキーマ不一致\|status.*adopted.*read_skipped" feedback/*.md` で確認したところ、2件の既出問題が今回も再現・継続していた。

### 1. `_seen.json` スキーマ不一致（既出・継続）
- **種類**: プロンプト記述 / ツール挙動
- **再確認**: 2026-05-29 feedback・2026-06-30 feedback で既に報告済み。プロンプトの「_seen.json の仕様 > スキーマ」節は `{version, updated_at, entries: [{url, fetched_at, status}]}` を規定しているが、実ファイル（今回確認時点で4184件）は依然としてフラット配列 `["url1", "url2", ...]` のまま。今回も実態（フラット配列）に合わせて append-only で URL のみ追記し、`adopted`/`read_skipped`/`fetch_failed` の区別は行わなかった（changelog 側で判定を記録）。
- **影響**: 4ヶ月以上未解消。「`fetch_failed` は24時間後に再試行可能」という運用ルールが実装上機能しない状態が続いている。
- **再現性**: 継続的（2026-05-06以降、確認された全実行で発生）。2026-06-30 feedback の提案（プロンプト記述を実態に合わせるか、実装をオブジェクト化するかの判断）はまだ反映されていない。

### 2. Stripe Engineering の `EGRESS_BLOCKED` が3回連続で再現（既出・継続）
- **種類**: アクセス（想定外挙動）
- **再確認**: 2026-09-07 feedback で初報告、2026-09-09 feedback で2回目の再現を確認済み。今回（2026-09-10）で**3回連続**の再現となり、単発の障害ではなく実行環境（tech-radar-curator-env）の恒常的な egress allowlist 制約であることがほぼ確定した。`stripe.com/blog/engineering` への301リダイレクト先 `stripe.dev/blog/topic/engineering` が allowlist 外。
- **提案する変更（再掲・優先度を上げて再提案）**: 「段階1をスキップするソース」表（Netflix Tech Blogが記載されている表）に Stripe Engineering も追加し、開始段階を「HTML特殊処理の該当ソースは egress allowlist に `stripe.dev` を追加するまで試行自体をスキップし、changelog に『既知の環境制約によりスキップ』とだけ記録する」という運用に変更する。現状は毎回同じ301リダイレクト→ブロックの実験を繰り返しているだけで新しい情報が得られていない。
- **想定リスク**: allowlist が将来変更されれば見逃しが発生するため、月1回程度の頻度で「スキップ解除を試す」チェック日を設けるなどの再有効化条件を明記すべき。
- **優先度**: 中（3回連続再現で確度が上がったため、2026-09-07時点の「低〜中」から引き上げ）

## 新たに観察した問題

### 3. 並列 fetch エージェント分割により全24ソース＋全トピック/タグを1サイクルで完走できた
- **種類**: プロンプト記述（ポジティブな観察）
- **詳細**: 過去の changelog（2026-08-19, 2026-09-09等）では時間制約により Zenn/Qiita/dev.to/Medium の一部トピックが「時間制約・未試行」として毎回欠落していた。今回は取得作業を4つの独立した並列タスク（官公式+国内+海外+Stripe / Zenn / Qiita+dev.to / Medium）に分割して同時実行したことで、全77ソース試行を1サイクル内に完走できた。
- **提案する変更**: プロンプトの「並列実行ルール」節に、標準RSSソースの取得作業自体を「情報源カテゴリ単位で独立したサブタスクに分割して並列実行してよい」という趣旨を明記する。現状の並列実行ルール表は個々のHTTPリクエストの並列数（5並列まで等）のみを規定しており、取得作業全体をカテゴリ単位で並列化する選択肢に触れていない。
- **理由**: 「時間制約・未試行」の欠落を構造的に解消できる可能性がある。
- **優先度**: 中

### 4. dev.to のタグフィードは直近ページ上限（約12件）を超える件数が窓内に存在する可能性がある
- **種類**: ツール挙動
- **発生箇所**: dev.to全15タグの取得（`nextjs`, `react`, `typescript`, `webdev` 等）
- **詳細**: 高頻度タグ（nextjs, react, typescript, webdev, ai, css, accessibility, performance, security, devops）は、フィードが返す12件全てが窓内（36h以内）に収まっており、フィードの取得上限自体が「直近36h全件取得」を妨げている可能性が高い（2026-06-30 feedbackでMediumについて同種の指摘が既出）。
- **想定リスク**: 高頻度タグでは実際にはより多くの候補記事が存在するが、フィード上限により機会損失している。
- **優先度**: 低（Medium分と合わせて、ページネーション対応の要否を今後まとめて検討する価値がある）

### 5. 「タイトルからの類推」による誤判定に注意が必要な事例を観測
- **種類**: 採用基準の迷い / 品質
- **発生箇所**: `zenn.dev/matsutake_prgrm/articles/dynamic-route-swallowed-dashboard` の事前分類
- **詳細**: タイトルと `@dashboard` という文言から Parallel Routes の話だと類推していたが、実際に本文を fetch したところ「静的ルート未作成時に動的ルート `[username]` が `/dashboard` を飲み込む」という全く別種のルーティング問題だった（Parallel Routes は無関係）。候補の絞り込み段階でタイトルだけから同一トピックと判断して pattern2 成立を仮定するのは危険で、本文確認前の「複数ソースで同一知見」という仮説は本文取得後に必ず再検証する必要がある。
- **想定リスク**: 本文未確認のまま pattern2 成立と誤判定し、実際には無関係な2記事を根拠に新規ルールを起票してしまうリスクがある。
- **優先度**: 中

## プロンプト改善提案（まとめ）

### 1. Stripe Engineering を「段階1をスキップするソース」表に準じた「既知の環境制約でスキップ」対象に追加する
- 上記フィードバック2参照。

### 2. 標準RSSソースの取得をカテゴリ単位で並列サブタスク化してよいことを明記する
- 上記フィードバック3参照。

### 3. `_seen.json` のスキーマ記述を実態（フラット配列）に合わせて修正する
- 2026-06-30 feedback からの再掲。4ヶ月以上未反映のため優先度を「中」に引き上げて再提案する。

## 取り上げなかったが気になる挙動

- Zenn/Qiita/dev.to/Medium 全体で、生成AIによる定型ダイジェスト記事のパターンが複数観測された（`fd_ai_teacher`のtech-YYYYMMDDHHMMSS形式、`bambampurin`の「今日の技術トレンド」シリーズ、dev.to `security`タグの`CVE Reports`による定型CVE速報、dev.to `devops`タグの`Biffer Rowley`による同一架空プロダクト「Shadow」を参照する専門用語サラダ的タイトル2連投）。個別の却下判断には反映済みだが、こうした「AI生成疑いの定型記事」を横断的にウォッチする仕組み（著者名リストのブラックリスト化等）があれば効率化できる可能性がある。
