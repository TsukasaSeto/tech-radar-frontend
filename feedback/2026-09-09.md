# Routine Feedback 2026-09-09

## 実行サマリ
- 採用: 1件 / 強化: 0件
- 採用前チェックでスキップ: 0件
- 取得失敗ソース: 2件（Stripe Engineering、Medium個別記事1本）
- 気づきレベル: 中

## 既出問題の再確認（grep結果）

`grep -rl "stripe.dev\|EGRESS_BLOCKED\|tech-radar-curator-env" feedback/*.md` で直近7日以内に同種の指摘が既出かを確認したところ、`feedback/2026-09-07.md` に同一事象（Stripe Engineering が `stripe.com/blog/engineering` → `stripe.dev/blog/topic/engineering` の301リダイレクトを経由し、リダイレクト先が実行環境のegress allowlist外で`EGRESS_BLOCKED`になる）が既に記録されている。今回のセッションでも全く同じ挙動（同じリダイレクト先・同じエラー種別）を再現した。**2026-09-07 → 2026-09-09 の2回連続で再現しており、単発ではなく実行環境の恒常的な制約であることが確定した**。2026-09-07 feedback の改善提案2（実行前にcurator-env前提を検証するステップを追加する）はまだ反映されていない模様（`.memo/prompt-v12.md` を今回のセッションでは未確認だが、少なくとも本セッションが受け取ったプロンプト本文には検証ステップは含まれていなかった）。判定の言い訳には使わず、Stripeは単にそのカテゴリ（HTML特殊処理ソース）を「取得失敗」として changelog に記録するのみに留めた。

## 新たに観察した問題

### 1. Medium個別記事の本文取得が3段階（A/B/C）とも失敗するケースを観測
- **種類**: アクセス（想定外挙動）
- **発生箇所**: 個別記事取得（`https://medium.com/@entepazhe/...`）
- **詳細**: 段階A（直接WebFetch）は403 Forbidden、段階B（AllOrigins raw）は500、段階C（AllOrigins get）は408 Timeout。Medium個別記事のRSS一覧自体（段階1）は問題なく200で取得できるが、個別記事本文はBot対策と思われる403が頻発する。今回はたまたま同テーマ（Next.js Hydration Error）の記事がdev.to/Zennで独立に2件見つかったため採用判定自体には影響しなかったが、Medium発の記事が単独で採用候補になった場合はこの経路で機会損失になりうる。
- **想定リスク**: Mediumタグ（nextjs/react/typescript/frontend/testing/performance/css/accessibility/security/i18n/devops/monorepo）は12種類あり、頻度は不明だが今後も同種の失敗が起きる可能性がある。
- **優先度**: 低〜中（今回は実害なし、観察記録のみ）

### 2. `fd_ai_teacher`（Zenn）の記事を根拠に含めるかどうかの判断
- **種類**: 採用基準の迷い
- **発生箇所**: Step 9 採用判定（Rule #7 追加時）
- **詳細**: 過去changelog（2026-08-11）に「AI生成の"tech-daily"定型記事で品質に疑義」と記録された著者`fd_ai_teacher`の記事が、今回パターン2成立に必要な「2人目の著者」として該当してしまった。URLパターン（`tech-YYYYMMDDHHMMSS-1`）から見て過去に品質懸念を指摘された同一の定型フォーマットと判断されるが、今回の記事は実際に読んだところ具体的なコード例・正確な技術内容（`useEffect`+マウント済みフラグパターン、`suppressHydrationWarning`の適用範囲限定など）を含んでおり、内容自体に誤りは見当たらなかった。今回はdev.to記事を一次根拠、Zenn記事を裏付けとして扱い、確信度「高」のまま採用したが、Zenn側1本を除外した場合は単独ソース（dev.toのみ）でパターン2不成立となり、B判定（新規ルール不可）に落ちる可能性があった。
- **提案する変更**: 「品質に疑義がある」と一度記録された著者/フィード（定型フォーマットのURLパターンを含む）については、パターン2の「異なる著者2記事」の片方としてのみ使う場合の扱い（許容する／別途3人目の裏付けを要求する／確信度を一段階下げる、等）を明文化する。
- **想定リスク**: 明文化しないと、レビューするセッションごとに判断がぶれ、同じ著者が絡む採用の確信度表記が一貫しなくなる。
- **優先度**: 中

### 3. 既存ルールとの「同じ趣旨か別角度か」の判定
- **種類**: 採用基準の迷い
- **発生箇所**: `practices/nextjs/server-components.md` Rule #6（ブラウザAPI直接アクセスで`ReferenceError`）と今回追加のRule #7（Hydration Mismatchの複数原因）の関係
- **詳細**: 両者は「ブラウザ専用の値をレンダーで直接使う」という表面的な共通点があるが、Rule #6はサーバー側の`ReferenceError`即時クラッシュ、Rule #7はSSR自体は成功するがクライアントDOMと不一致になる無音の警告、という別の失敗モードである。Step 9 のチェック4「トピック重複チェック」を行った上で、「別の角度から扱う」に該当すると判断し独立ルール化したが、迷った場合は強化に倒すという原則との兼ね合いで判断に時間を要した。
- **提案する変更**: 「同じ趣旨 vs 別角度」の判定基準に、「失敗の観測可能な症状（例外/クラッシュ vs 警告/サイレント）が異なる場合は別角度とみなしてよい」といった具体例を1つ追記する。
- **優先度**: 低

## 取り上げなかったが気になる挙動

- Medium `nextjs` / `react` / `typescript` タグにおいて、`blogbuyguide` / `syamkumar0418` / `connect2syam` という3著者が同日同時間帯に「Next.js SaaSダッシュボード」系の類似テーマ記事を大量投稿していた（Dark Mode, Data Tables, Sidebar Navigation, RBAC, Multi-Tenancy等）。内容の質を精読はしていないが、投稿パターン（同日連続・似たタイトル構造）からSEO目的の量産コンテンツである可能性が高く、本文取得を見送った。今後Mediumの`nextjs`タグ全体でこの傾向が続く場合、dev.toの`webdev`/`ai`/`security`タグと同様に「低品質記事が多い」という注意書きを追加する価値があるかもしれない。
- dev.to `security` タグは前回（2026-09-07）に続き今回も大半が暗号資産・CVE速報・スパム風記事で占められていた（10件中、フロントエンド関心領域に該当するものは実質0件）。継続的な傾向として確定しつつある。
