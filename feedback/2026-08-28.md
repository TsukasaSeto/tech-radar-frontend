# Routine Feedback 2026-08-28

## 実行サマリ
- 採用: 4件 / 強化: 5件（うち1記事から2ルール強化）
- 採用前チェックでスキップ: 0件
- 取得失敗ソース: 1件（Stripe Engineering。標準RSS 75フィードは全て段階3までで到達）
- 気づきレベル: 中

## 観察した問題（6件）

### 1.（継続・11日連続）Stripe Engineering が全段階失敗
- **種類**: アクセス / プロンプト記述（環境設定）
- **発生箇所**: Step 5 HTML特殊処理ソース（Stripe Engineering）
- **詳細**: `feedback/2026-08-13` 以降 `2026-08-24` まで連続報告されている既知パターンが本日も再現。`stripe.com/blog/engineering` は `stripe.dev/blog/topic/engineering` へ 301 し、リダイレクト先が許可ドメイン外で egress proxy が `CONNECT tunnel failed, response 403` を返す。AllOrigins raw / get はいずれも HTTP 522（Cloudflare）。2026-08-24 と同じく**全段階失敗**で、段階4での回復も効かなかった。
- **影響**: Stripe Engineering から3週間以上一覧取得ができていない。ソース定義が実質デッドリンク化している。
- **再現性**: 継続的（少なくとも10回連続報告）。
- **提案**: 「ソース定義から Stripe を外す」か「許可ドメインに `stripe.dev` を追加する」かを人間側で決着させる。毎日 feedback に同じ項目が積まれる状態自体がノイズになっている（2026-08-22 で優先度「高」に格上げ済み、以降も未対応）。

### 2.（新規）OWASP CheatSheetSeries の atom フィードが段階1で HTTP 403 になった
- **種類**: アクセス
- **発生箇所**: Step 4、`https://github.com/OWASP/CheatSheetSeries/commits/master.atom`
- **詳細**: 段階1（直接取得）で HTTP 403（レスポンスボディ378バイト）。GitHub 側の UA / レート制限によるものと見られる。段階2（rss2json）では `status=ok` で10件取得できたため実害は回避できた。過去の feedback（2026-08-05 / 08-23）で OWASP は言及されているが、403 の観測は今回が初。
- **影響**: なし（段階2で回復）。ただし単発か常態化かは次回以降の観測が必要。
- **再現性**: 初回観測。
- **提案**: 3回以上連続で 403 が観測されたら、プロンプトの「段階1をスキップするソース」表に Netflix と並べて OWASP を追記する（今回は単発の可能性があるため追記は見送った）。

### 3.（継続）Qiita `accessibility` タグフィードが段階3でしか回復せず、しかも中身が空
- **種類**: アクセス
- **発生箇所**: Step 4、Qiita accessibility タグ
- **詳細**: 段階1 HTTP 404 → 段階2 rss2json HTTP 500 → 段階3 AllOrigins raw HTTP 200（584バイト、`<entry>` 0件）。2026-08-19 / 08-22 / 08-24 に続き4回目の同一パターンで、レスポンスサイズまで前回と同一。
- **影響**: accessibility 記事を Qiita から4週間近く収集できていない。段階1〜3を毎回消費するだけの空振りになっている。
- **再現性**: 継続的（4回観測）。
- **提案**: フィードURL自体が無効化されたと判断し、標準RSSソース定義の Qiita タグ一覧から `accessibility` を削除する（dev.to / Zenn 側の accessibility タグは生きているためカバレッジ上の損失は限定的）。

### 4.（継続・6日超）一次トリアージが依然としてプロンプト未記載のまま
- **種類**: プロンプト記述
- **発生箇所**: Step 8「個別記事の本文取得」
- **詳細**: 本日の窓内候補は 377件（URL正規化・重複排除後 344件）。Step 8 の文言は全件への段階A→C適用を示唆するが、実行時間内には不可能なため、本日もタイトル・トピックからの一次トリアージで10件に絞ってから本文取得した。2026-08-18/19/21/22/23/24 と同一の指摘であり、提案（Step 7.5 の新設 or Step 8 冒頭への閾値明記）も複数回出ているが未反映。
- **影響**: トリアージで機械的に見送った記事にパターン1/2 該当記事が含まれていた可能性を排除できない。判断基準がセッションごとに揺れるため再現性がない。
- **再現性**: 継続的（7日連続）。
- **補足**: 本日は暫定対応として changelog に「一次トリアージ」節を新設し、絞り込み基準（公式ソース優先 → パターン1c候補 → 既存ルールの反例）と候補件数を明記した。プロンプト側に取り込むならこの3段の優先順位をそのまま使えるはず。

### 5.（新規）36h 窓が「重要な公式セキュリティリリース」を構造的に取りこぼす
- **種類**: プロンプト記述（採用基準・期間）
- **発生箇所**: 「期間とスコープ」／Step 3「公式リポジトリの差分確認」
- **詳細**: Step 3 で vercel/next.js の Releases を確認したところ、**v16.3.3 / v15.5.24（2026-08-25）が「Windows ホストの未認証 RCE」「Image Optimization API（AVIF）の未認証 RCE」の2件を修正するセキュリティリリース**であることが判明した。しかし公開が 36h 窓（2026-08-27T09:08Z 以降）より前のため、絶対ルール1・期間スコープに従うと本日のルール化対象にならない。直近の Routine 実行は 2026-08-24 が最後（`practice-update/2026-08-24`）で、8/25〜8/26 に実行が無かった結果、**このセキュリティリリースはどの日の窓にも入らず取りこぼされている**。
- **影響**: `practices/nextjs/` 配下は「A: セキュリティ → B: アーキテクチャ → C: パフォーマンス」の優先順位を掲げているにもかかわらず、最優先カテゴリの情報が窓の隙間から抜け落ちる。Routine が1日でもスキップされると同じ穴が空く。
- **再現性**: 構造的（Routine 未実行日が発生するたびに再発）。
- **提案**: Step 3（公式リポジトリの差分確認）だけは 36h 窓の対象外とし、「前回 changelog の日付以降にリリースされた Next.js / React / TypeScript のリリースは、36h 窓外でも採用検討対象とする」旨を明記する。Step 3 は RSS ではなく `_seen.json` でも管理していないため、窓を外しても重複採用のリスクは低い。
- **本日の暫定対応**: changelog の「Step 3 メモ」に宿題として記載し、`typescript/strict-mode.md` の TS 7.0 表記（7.0.2 リリース済み）だけは能動的照合ルールを根拠に更新した。Next.js の RCE 修正はルール化していない。

### 6.（継続・構造的）`_seen.json` の実ファイル形式とプロンプト記載スキーマの乖離、および commit 直前 grep の誤検知
- **種類**: プロンプト記述 / ツール挙動
- **詳細**: 実ファイル（本日時点 3,864件 → 3,874件）は引き続き `["url1", "url2", ...]` のフラット配列で、プロンプト記載の `{version, updated_at, entries:[{url,fetched_at,status}]}` スキーマとは異なる。2026-08-23 / 08-24 で詳細報告済み。本日も実ファイル形式（フラット配列 + append-only + 順序保持 unique）に合わせて追記し、`diff <(sort base) <(sort new)` による削除検知と `uniq -d` による重複検知の2種を実施して 0件を確認した。
- また 2026-08-24 の指摘どおり、`git diff --cached _seen.json | grep '^-  "'` は JSON 配列末尾に追記すると「最後の要素にカンマが付く」ため必ず1行ヒットし、実際のURL削除と区別できない。本日も sort 済み差分検証で 0件ドロップを確認した上で継続した。
- **再現性**: 構造的（新規情報なし。詳細は 2026-08-23 / 08-24 参照）。

## プロンプト改善提案（3件）

### 1. Step 3 を 36h 窓の対象外にする（新規・優先度 高）
- **対象セクション**: 「期間とスコープ」および「Step 3: 公式リポジトリの差分確認」
- **現状の記述**: 「対象期間: 実行開始時刻 UTC を基準として `now - 36h` 以降に公開された記事」（全ステップに一律適用される読み方）
- **提案する変更**: Step 3 の冒頭に追記する:「Step 3 で確認する公式リリース（vercel/next.js Releases、facebook/react CHANGELOG、microsoft/TypeScript Releases）については 36h 窓を適用せず、**リポジトリ内の直近 changelog の日付以降**にリリースされたものを全て採用検討対象とする。特にセキュリティ修正を含むリリース（CVE / RCE / 権限昇格）は、窓外であっても `practices/nextjs/` 等の該当ファイルに反映する。既に反映済みかどうかは `grep -rn "<バージョン番号>" practices/` で判定する。」
- **理由**: 2026-08-25 の Next.js RCE 修正2件が、Routine 未実行日（8/25〜8/26）の存在によりどの窓にも入らず取りこぼされた。セキュリティ情報の取りこぼしは `practices/nextjs/README.md` の優先順位（A: セキュリティ最優先）と正面から矛盾する。
- **想定リスク**: 窓を外すと同じリリースを複数日にわたり再検討する可能性がある。`practices/` の grep による重複チェックで抑止できる。
- **優先度**: 高

### 2. Qiita `accessibility` タグを標準RSSソース定義から削除する（優先度 中）
- **対象セクション**: 「標準RSSソース定義 > コミュニティ系 > Qiita」
- **現状の記述**: `tag: next.js, react, typescript, jest, playwright, claudecode, css, accessibility, performance, ...`
- **提案する変更**: `accessibility` を削除し、脚注として「Qiita の `accessibility` タグフィードは 2026-08-19 以降 HTTP 404 が継続し、プロキシ経由でも 0 エントリのため定義から除外した」と残す。
- **理由**: 4回連続で同一の失敗パターン（404 → 500 → 200/0件）。毎回3段階を消費するだけで収集成果がない。
- **想定リスク**: Qiita 側が復旧した場合に再追加が必要。脚注に日付を残しておけば判断可能。
- **優先度**: 中

### 3.（再掲・7日連続）Step 8 の一次トリアージを明文化する（優先度 高）
- **対象セクション**: 「Step 8: 個別記事の本文取得」
- **提案する変更**: 2026-08-23 / 08-24 の提案と同趣旨。冒頭に「窓内候補が概ね50件を超える場合、(1) 公式ソース由来、(2) 公式ツールの特定バージョン挙動を具体的な設定・API名で扱うもの（パターン1c候補）、(3) 既存プラクティスの反例・実測値を示すもの、の順に優先して上限25件まで本文取得する。個別取得しなかった記事は `_seen.json` に記録しない。候補総数・精読件数・絞り込み基準を changelog に明記する」を追加。
- **理由**: 7日連続で同じ運用が発生しているのにプロンプト未反映。本日は changelog 側に「一次トリアージ」節を設けて暫定対応したが、基準がプロンプトにないままではセッションごとに揺れる。
- **想定リスク**: 上限値（25件）は恣意的。ただし明記しないと現状維持。
- **優先度**: 高（7日連続のため据え置き）

## 取り上げなかったが気になる挙動

- **Mercari のフィードが復旧した**: 2026-08-24 に「全段階失敗」と報告した `https://engineering.mercari.com/blog/feed.xml` は、本日は段階1で HTTP 200（約1.9MB）を返した。窓内の新着は0件。障害が一時的だったのか間欠的なのかは判断材料が足りない。
- **`devblogs.microsoft.com` が EGRESS_BLOCKED**: TypeScript 7.0 の公式アナウンス本文を裏取りしようとしたが遮断された。GitHub Releases 側でリリース日とタグ（7.0.2 / 2026-08-20）は確認できたため確信度「中」で採用したが、TypeScript 系ルールの一次情報が取れない状態が続くなら許可ドメインへの追加を検討する価値がある（`typescript/strict-mode.md` Rule #9 の確信度が「中」に留まっている直接の原因もこれ）。
- **`practices/ai-agent/claude-code.md` が46ルールに到達**: 2026-08-24 の feedback でも指摘されているとおり、ファイル分割（権限管理系の切り出し等）を検討する時期に来ている。本日追加した Rule #46 も権限管理系で、既存の Rule #36 / CVE-2026-33068 の記述と近接トピックだった（主題が異なるため新規採用としたが、判断は微妙だった）。
