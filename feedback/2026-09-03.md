# Routine Feedback 2026-09-03

## 実行サマリ
- 採用: 2件 / 強化: 0件
- 採用前チェックでスキップ: 0件
- 取得失敗ソース: 1件（Stripe Engineering、全段階失敗）
- 気づきレベル: 中

## 観察した問題（4件）

### 1.（継続・悪化なし）Stripe Engineering の `stripe.dev` EGRESS_BLOCKED が本日も全段階失敗
- **種類**: アクセス / プロンプト記述（環境設定）
- **発生箇所**: Step 5 HTML特殊処理ソース（Stripe Engineering）
- **詳細**: `feedback/2026-08-24.md` 時点で「少なくとも9回連続報告」とされていたのと同一パターンが本日も再現した。`stripe.com/blog/engineering` は301で `stripe.dev/blog/topic/engineering` にリダイレクトされ、リダイレクト先が許可ドメイン外（許可リストは `stripe.com` のみで `stripe.dev` を含まない）のため `EGRESS_BLOCKED`。フォールバックの AllOrigins raw は HTTP 408（timeout）、AllOrigins get は HTTP 500 で、いずれも回復しなかった。
- **影響**: Stripe Engineering から少なくとも1ヶ月以上、正常な一覧取得ができていないことになる。エラーの具体的なステータスコード（408/500）は日によって変動するが、根本原因（stripe.com → stripe.dev への恒久的なドメイン移行）は変わらないため、リトライで回復する見込みは薄い。
- **再現性**: 継続的、10回以上連続報告と見られる。
- **提案（再掲）**: 許可ドメインリストに `stripe.dev` を追加するか、Stripeソースを標準RSSと同様の「継続的失敗として段階1をスキップし低頻度リトライに切り替える」運用（Netflixに対して既に採られている運用と同様）に変更することを推奨。ドメイン許可はこのセッションの権限では変更できないため、`.memo/env-tech-radar-curator.md` 側の手動対応が必要。

### 2.（新規）本Routineが専用cloud環境ではなく通常のリポジトリセッションから実行され、かつツール呼び出し予算の制約から76フィード中27フィードのみ試行した
- **種類**: プロンプト記述 / 環境設定
- **発生箇所**: 実行環境全体、Step 4 標準RSSソース取得
- **詳細**: `feedback/2026-08-24.md` の観察5と同じく、本Routineは `tech-radar-curator-env` 専用環境ではなく通常の `tech-radar-frontend` リポジトリセッションから実行された（今回で少なくとも2回目の観測）。加えて今回は、事前に「実質的なツール呼び出し予算を考慮し、全24〜76ソースへの5段階フォールバックを網羅するより、幅（できるだけ多くのソース種別に到達すること）を優先してよい」という明示的な制約下での実行だった。結果として、コミュニティ系4サイト（Zenn/Qiita/dev.to/Medium）は各サイト代表トピック「nextjs」のみを試行し、残り約52フィード（Zenn 14トピック、Qiita 13タグ、dev.to 14タグ、Medium 11タグ）を時間制約・未試行のまま終えた。公式・国内・海外の20ソース + Stripe は全て試行した。
- **影響**: 実害はないが（採用したルールは全て実際にfetch成功した記事に基づく）、実務上のカバレッジは過去の通常運用（76フィード全走査）より大幅に狭い。security/testing/accessibility/graphql等の専門タグから採用候補になり得た記事を機械的に見逃している可能性がある。
- **再現性**: 環境要因（cloud Routine由来かローカルセッション由来か）に依存するため不明。ツール呼び出し予算による絞り込みは今回のセッション固有の制約に起因。
- **提案**: 通常セッションから実行する場合でも、コミュニティ系4サイトは最低でも "nextjs, react, typescript, security" の4トピック程度は試行することを最低ラインとして明文化すると、幅と深さのバランスが再現可能になる。

### 3.（継続・新情報なし）`_seen.json` の実ファイルはプロンプト記載のオブジェクトスキーマと異なり、フラットなURL配列のまま
- **種類**: プロンプト記述
- **詳細**: 実ファイル（本日時点3864件→3869件）は引き続き `["url1", "url2", ...]` のフラット配列で、`status`/`fetched_at` は保持されない。2026-08-23/24で詳細報告済みのため簡潔に記載。今回も実ファイルの形式（フラット配列 + append-only + 順序保持unique dedupe + sort済み差分検証）に合わせて追記し、削除検知は0件だった。
- **再現性**: 継続的。

### 4.（新規）採用基準の「層判定保留」が発生する具体例が `ai-agent` カテゴリで見つかった
- **種類**: プロンプト記述
- **発生箇所**: Step 9 採用判定、Step 10 レイヤー役割マップ
- **詳細**: GitHub公式Engineering Blogの記事（GitHub Copilotのツール出力コスト最適化に関する内部知見、コード例なし）を検討したが、`practices/ai-agent/claude-code.md`（Claude Code固有）にも `practices/ai-agent/ai-sdk.md`（Vercel AI SDKでのアプリ内エージェント構築）にも scope が一致しなかった。Step 10 の「役割マップ」表はロギング/エラー処理/キャッシュ/依存管理/レンダリングの5トピックのみをカバーしており、ai-agent配下2ファイルの住み分け（ツール固有 vs SDK/アプリ構築）は明文化されていない。今回は無理に配置せず却下（D）としたが、今後「特定ベンダーに紐づかないAIコーディングエージェント設計原則」系の記事が増えると、同様の判定保留が繰り返される可能性がある。
- **影響**: 実害なし（今回は却下で処理した）。ただし今後同種の記事が増えると `ai-agent/` に3つ目のファイル（例: `agent-harness-design.md` のような汎用設計原則ファイル）が必要になるかもしれない。
- **再現性**: 今回が初観測。
- **提案**: 判断材料が今回のみのため、プロンプト改善提案には格上げしない（下記「取り上げなかったが気になる挙動」に記載）。

## プロンプト改善提案（1件）

### 1. コミュニティ系4サイトの最低試行トピック数を明文化する
- **対象セクション**: 「標準RSSソース定義（24ソース）> コミュニティ系（4サイト）」および「実行環境の前提」
- **現状の記述**: 各サイトのトピック/タグ一覧が列挙されているのみで、時間制約時にどのトピックを優先すべきかの指示がない
- **提案する変更**: 「ツール呼び出し予算の制約で全トピックを試行できない場合、最低限 `nextjs, react, typescript, security` の4トピック（Qiitaは `next.js, react, typescript, security`）を優先して試行すること。未試行のトピックはchangelogに『時間制約・未試行』として明示する」を追記する
- **理由**: 観察2参照。今回は代表トピック1つのみに絞ったが、次回セッションが同じ制約に直面した際の判断基準が存在しない
- **想定リスク**: なし（下限を明記するだけで、予算に余裕がある場合はこれまで通り全トピック走査を妨げない）
- **優先度**: 中

## 取り上げなかったが気になる挙動

- 観察4（ai-agentカテゴリの層判定保留）は今回が初観測のため、プロンプト改善の提案には格上げしなかった。今後同種の却下判断が2回以上続くようなら、`ai-agent/` 配下のファイル分割またはStep 10役割マップへの追記を検討する価値がある。
- 本日の候補記事の多く（dev.to の個人ブログ・Medium の個人ブログ計約19件）は、タイトルからの一次判断で明確に個人開発体験記/自己宣伝記事と分かるものが大半で、深いfetchを行わなかった。これは `feedback/2026-08-24.md` 観察3で既に報告済みの傾向と同じであり、新規の指摘には該当しない。
