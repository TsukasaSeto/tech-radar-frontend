# Routine Feedback 2026-08-30

## 実行サマリ
- 採用: 0件 / 強化: 3件
- 採用前チェックでスキップ: 0件
- 取得失敗ソース: 2件（Qiita accessibility、Stripe Engineering Blog）
- 気づきレベル: 重大

## 観察した問題（5件）

### 1. `stripe.com/blog/engineering` の `stripe.dev` リダイレクト起因の EGRESS_BLOCKED が本日も再現（少なくとも2週間以上、継続報告中）

- **種類**: アクセス / プロンプト記述
- **発生箇所**: Step 5（Stripe Engineering Blog 取得）
- **詳細**: `feedback/2026-08-17.md` で初めて明示的に報告された「`stripe.com/blog/engineering` が `stripe.dev` へ301リダイレクトされ、`stripe.dev` が許可ドメイン外で `EGRESS_BLOCKED` になる」問題が、本日（2026-08-30）も一字一句同じ症状（HTTP 301 → `stripe.dev` → `EGRESS_BLOCKED`、その後の AllOrigins raw/get フォールバックも HTTP 520/522 で全滅）で再現した。少なくとも 2026-08-13 から今日まで継続していることが過去 feedback から確認できる。
- **影響**: Stripe Engineering Blog が2週間以上にわたり実質的に取得不能。「新着なしは正常」という判定と「取得自体ができない」という異常が、この状態のままでは区別できない
- **再現性**: 継続的（2026-08-13 以降、確認できる限り毎回）
- **プロンプト改善提案**:
  - **対象セクション**: 「実行環境の前提 > 許可ドメイン」
  - **現状の記述**: `HTML特殊処理用: stripe.com`
  - **提案する変更**: `stripe.dev` を許可ドメインに追加する（`stripe.com/blog/engineering` は恒久的に `stripe.dev/blog/topic/engineering` へ301リダイレクトされる仕様と推測され、`stripe.com` だけの許可では届かない）
  - **理由**: 2週間以上同一の失敗が継続しており、Routine内部の対応（フォールバック段数を増やす等）では解決不可能。許可ドメインリストの変更はスケジューラ/カスタム環境設定側でしか行えない
  - **想定リスク**: なし（許可ドメイン追加は安全側の変更）
  - **優先度**: 高

### 2. `_seen.json` のスキーマ不一致が依然未解消（過去 feedback で18回以上報告済み、19回目以降）

- **種類**: プロンプト記述
- **発生箇所**: 「`_seen.json` の仕様 > スキーマ」節
- **詳細**: プロンプトは `{version, updated_at, entries:[{url, fetched_at, status}]}` を規定するが、実ファイル（`origin/main`）は素朴な URL 文字列のフラット配列（本日時点 3864件）。本日も実ファイル形式に従い、base取得→新規4件末尾append→順序保持unique→diff検証（削除なし）→重複検証（重複なし）を実施し、いずれも異常なし（3864件→3868件）
- **影響**: `status: adopted/read_skipped/fetch_failed` の記録ができないため、「読んだが採用しなかった」記事と「読んですらいない」記事が `_seen.json` 単体からは区別できない。本日はこの制約を踏まえ、実際に本文を fetch して読了した4件のみを `_seen.json` に追加し、タイトルのみ確認した約296件は追加しなかった（詳細は「観察した問題 3」参照）
- **再現性**: 継続的（過去 feedback で18回以上報告済み）
- **プロンプト改善提案**: 過去提案の再掲のみ（プロンプト側のスキーマ定義をフラット配列に修正するか、実ファイルを一度だけ object 形式にマイグレーションする）。18回超の報告でも未対応のため、次のアクションは人間側での意思決定（スキーマ変更を見送る/実施する）だと考える
- **優先度**: 高

### 3. 直近36h窓内の候補記事数が約300件と、通常想定より大幅に多く、全件精読が時間内に不可能だった

- **種類**: プロンプト記述 / その他
- **発生箇所**: Step 6-9（候補抽出〜採用判定）
- **詳細**: Zenn 88件、Qiita 約30件、dev.to 95件、Medium 90件の合計約300件が直近36h窓内でヒットした（`_seen.json` 重複除外後）。本セッションで実際に本文を fetch して精読したのは4件のみ。残り約296件はタイトル・著者・投稿日のメタデータのみでの一次トリアージ（明らかな関心領域外・スパム・既存ルールとの重複可能性）にとどまり、本文は未読
- **影響**: 統計的に見て、296件の中に本来 A/B 判定に値する記事が含まれていた可能性は否定できない。今回は「既存ルール見出しとの重複可能性が高い候補」を優先したため、既存の強いテーマ（AIエージェント/Claude Code関連が今回だけで約40件）に判定が偏っている可能性がある
- **再現性**: 単発（過去 feedback ではここまでの候補数増加は確認していないが、`feedback/2026-08-17.md` でも「時間制約により未着手: 25件」の前例あり。原因不明だが、本日はコンテナ再起動によるやり直しで実行時間の一部を消費した影響も考えられる）
- **プロンプト改善提案**:
  - **対象セクション**: Step 8「個別記事の本文取得」
  - **提案する変更**: 候補記事数が閾値（例: 100件）を超えた場合の優先順位付けルールを明示する（例: 公式ソース→パターン1c該当候補→既存ルール見出しとの重複候補→残りはランダムサンプリング、等）。現状は暗黙の判断に委ねられており、本日のような偏りが発生しうる
  - **理由**: 候補数が想定を超えた日の判定品質を再現可能にするため
  - **想定リスク**: 優先順位付けルール自体が別の偏りを生む可能性がある
  - **優先度**: 中

### 4. GitHub Releases API（`api.github.com`）への未認証アクセスが HTTP 403 で失敗

- **種類**: アクセス
- **発生箇所**: Step 3（公式リポジトリの差分確認）
- **詳細**: `https://api.github.com/repos/vercel/next.js/releases` と `https://api.github.com/repos/microsoft/TypeScript/releases` がいずれも HTTP 403 を返した（2回試行）。`raw.githubusercontent.com` 経由の `facebook/react/CHANGELOG.md` は成功しており、`api.github.com` のみが失敗している点から、レート制限（未認証リクエストの上限）の可能性が高い
- **影響**: Next.js / TypeScript のメジャーバージョン・非推奨化情報の能動的照合（プロンプト「C. 既存ルール修正の条件」で要求されている）が今回実施できなかった
- **再現性**: 不明（初観測）
- **プロンプト改善提案**:
  - **対象セクション**: Step 3
  - **提案する変更**: `api.github.com/repos/.../releases` の代わりに `github.com/.../releases.atom`（認証不要な RSS 相当エンドポイント）を第一候補にする、または GitHub MCP でのアクセス（本セッションでは本リポジトリ単体にスコープが限定されており利用不可だったが、環境によっては使える可能性がある）を明示的な代替経路として追記する
  - **優先度**: 中

### 5. Zenn記事の本文中に、設定ファイル形式に類似した指示的テキストが含まれ、ハーネス側で自動的に無害化された

- **種類**: ツール挙動 / その他
- **発生箇所**: Zenn `claudecode`/`security` トピック収集中（`permissions.allow` を扱う記事のタイトル一覧化時）
- **詳細**: 複数の Zenn 記事タイトル（例:「permissions.allow検証をClaudeに頼んだら...」）が Claude Code の `settings.json` の `permissions.allow`/`deny` 設定に関する内容を扱っており、これらの記事内容を経由したサブエージェント出力に対して、ハーネス側が「instruction-shaped pattern（settings-json, permissions-allow-deny）」を検知し、該当箇所を自動的に中立化（`<` → `<\``）する処理が働いた
- **影響**: 実害はなし（該当記事はいずれも候補一覧化のみで、本文精読・ルール抽出の対象にはしていない）。プロンプトインジェクションの試みというよりは、記事の主題そのもの（Claude Code の権限設定を検証する内容）が偶然パターンに合致した可能性が高いと考えられるが、断定はできない
- **再現性**: 単発（初観測）
- **プロンプト改善提案**: なし（Routine 側で対処すべき事項ではなく、ハーネスの安全機構が正しく機能した事例として記録するのみ）
- **優先度**: 低

## 取り上げなかったが気になる挙動

- 未マージの Curator PR が3日分（2026-08-27〜2026-08-29、計6PR）滞留している（`#222`〜`#227`）。main 上の最新 changelog/feedback は 2026-08-24 時点で止まっている。Routine 自体はマージ作業の範囲外だが、日次実行が積み重なるほど `_seen.json` の base 取得元（`origin/main`）が古くなり、翌日以降の重複判定の精度に影響する可能性がある点は留意が必要
- 本日はセッション途中でコンテナが再起動し、5並列で走らせていたフェッチ用サブエージェントのうち1つ（JP/global ブログ・Linear・Stripe・公式リリース確認担当）の結果が失われ、再実行が必要になった。実行時間の増加要因になっている
