# Routine Feedback 2026-08-31

## 実行サマリ
- 採用: 2件 / 強化: 4件
- 採用前チェックでスキップ: 0件
- 取得失敗ソース: 1件（Stripe Engineering）
- 気づきレベル: **重大**

## 観察した問題（3件）

### 1.（継続・重大）マージされていない Routine PR が 8 本、6 日分滞留している

- **種類**: プロンプト記述 / 運用
- **発生箇所**: リポジトリ全体（`main` ブランチの状態）
- **詳細**: 本Routine実行の直前に確認したところ、`main` へのマージは 2026-08-24 の `practice-update/2026-08-24` + `seen-json-update/2026-08-24`（コミット `ae2b855` / `5b4d9e4`）が最後だった。一方で cloud 側の Frontend Practice Curator Routine 自体は毎日休まず実行され続けており、`practice-update/2026-08-27`〜`2026-08-30` と対応する `seen-json-update/*` の計 **8 PR（#222〜#229）が open のまま滞留**している（2026-08-25・08-26 分は PR 自体が見当たらず、fetch失敗等で見送りになった可能性がある）。このリポジトリの CLAUDE.md は「この repo での Claude Code セッションの大半は Routine が生成した PR の取り込み」と明記しており、`routine-followup` スキルによる定期的な取り込みが前提の設計だが、直近 1 週間はこれが行われていない。
- **影響**: 8 PR 分の practices 追加・強化・`_seen.json` 更新が `main` に未反映。日を追うごとに `_seen.json` の base がずれてコンフリクトリスクが増大し、`practices/` の Rule 番号衝突（CLAUDE.md が「常連」と警告する `practices/ai-agent/claude-code.md` 等）が発生する確率も上がる。
- **再現性**: 継続的（放置すればするほど悪化する）。
- **提案**: 次にこのリポジトリで人間または Claude Code セッションが作業する際は、まず `routine-followup` スキルで #222〜#229 の取り込みを最優先で行うことを強く推奨する。

### 2.（継続）本Routineが `tech-radar-curator-env`（cloud Routine専用環境）ではなく、通常のリポジトリセッションから実行された

- **種類**: プロンプト記述 / 環境設定
- **発生箇所**: 実行環境全体
- **詳細**: 2026-08-24 の feedback で報告済みの事象が本日も再発した。本Routineは今回もスケジュール実行された通常の `tech-radar-frontend` リポジトリセッション上で実行され、cloud Routine 専用環境（`.memo/env-tech-radar-curator.md` が想定する環境）ではなかった。必要な全ドメイン（zenn.dev / qiita.com / dev.to / medium.com / api.allorigins.win / github.com / vercel.com 等）への egress は問題なく成功し、PR作成まで完遂できた（Stripe→stripe.devのみ既知のブロック）。
- **影響**: 上記「気づき1」と合わせて考えると、**cloud側 Routine と、このスケジュール済みリポジトリセッション側 Routine が、同一日に重複して `practice-update/2026-08-31` を起票するリスクが理論上ある**（cloud側の当日実行がまだのタイミングだったため、本日は `practice-update/2026-08-31` ブランチが存在しないことを確認した上でPRを作成した）。今後も同じスケジュールが両方から実行され続けると、いずれ同日ブランチの衝突が発生しうる。
- **再現性**: 継続的（2026-08-24, 2026-08-31 の少なくとも2回観測）。
- **提案**: このリポジトリで「Frontend Practice Curator Routine」というスケジュールタスクが cloud env と通常リポジトリセッションの両方に設定されていないか、ユーザー側で確認することを推奨する。もし重複設定であれば、通常リポジトリセッション側は `routine-followup`（PR取り込み）用のスケジュールに変更するのが CLAUDE.md の設計意図と一致する。

### 3. `_seen.json` の実データはフラット配列のまま（既知の乖離、再確認のみ）

- **種類**: プロンプト記述
- **発生箇所**: `_seen.json` の仕様セクション
- **詳細**: 2026-08-23/08-24 で詳細報告済みの通り、実ファイルは引き続き `["url1", "url2", ...]` のフラット配列で、`status`/`fetched_at` を持つオブジェクトスキーマではない（本日時点 3864件→3869件）。今回も実データの形式に合わせて append-only + 順序保持unique で追記した。
- **影響**: 実害なし（過去報告済みの通り運用でカバーできている）。
- **再現性**: 継続的（新規観察ではない）。

## プロンプト改善提案（1件）

### 1. スケジュールタスクの実行環境を明示的に確認するステップを先頭に追加する

- **対象セクション**: 「実行環境の前提」
- **現状の記述**: 「このRoutineは custom env (tech-radar-curator-env) で実行されることを前提とする」とあるのみで、実行時にその前提が満たされているかを確認する手順がない
- **提案する変更**: Step 1 の前に「現在の実行環境が `tech-radar-curator-env` かどうかを確認し、通常のリポジトリセッションで実行されている場合はその旨を feedback に明記した上で続行する」という軽量チェックを追加する
- **理由**: 2026-08-24, 2026-08-31 と2回連続で同じ環境不一致が観測されており、cloud側との重複実行リスクが看過できない段階に来ている
- **想定リスク**: チェック自体が誤検知すると本来の実行まで止めかねないため、「続行を妨げない・記録するだけ」の軽量チェックに留めるべき
- **優先度**: 高
