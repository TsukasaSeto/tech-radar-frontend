# Routine Feedback 2026-08-19

## 実行サマリ
- 採用: 3件 / 強化: 7件
- 採用前チェックでスキップ: 1件
- 取得失敗ソース: 2件（Qiita `accessibility`、Stripe Engineering Blog）
- 気づきレベル: 中

## 観察した問題（3件）

### 1. dev.to/Medium のタグ別フィードの「直近ページのみ」制約が実害段階に達した（初報告2026-08-18「取り上げなかったが気になる挙動」からの格上げ）
- **種類**: アクセス / プロンプト記述
- **発生箇所**: Step 4 標準RSSソース取得、Medium 12タグ
- **詳細**: 本日 Medium の `react`/`typescript`/`testing`/`performance`/`css`/`accessibility`/`security`/`devops` の8タグで、返された10件全てが直近36h窓内だった。これは「投稿頻度がフィードの返却上限（10件）を上回っている」ことを意味し、窓の後半（古い側、つまり cutoff に近い時刻）の記事が構造的に取得できていない。2026-08-18のfeedbackでは「気になる挙動」（未確定の推測）として記録したが、本日は8/12タグで飽和が確認され、推測ではなく確定した構造的欠落だと判断できる
- **影響**: 直接的な誤採用はないが（結果的に低品質記事が大半だったため実害は小さい）、良質な記事が窓の後半に埋もれて見逃されている可能性を否定できない
- **再現性**: 継続的（2026-08-18で兆候を記録、本日確定）

### 2. Stripe Engineering Blog の `stripe.dev` リダイレクトが再び全段階失敗（2026-08-13, 08-15, 08-18 からの継続）
- **種類**: アクセス
- **発生箇所**: Step 5 HTML特殊処理ソース
- **詳細**: `stripe.com/blog/engineering` → `stripe.dev/blog/topic/engineering` への301リダイレクトが許可ドメイン外でブロックされる問題は既に複数回報告済み。2026-08-18はAllOriginsのリトライで回復したが、本日はAllOrigins raw も522を返し、フォールバックも含めて完全失敗した
- **影響**: 直近36hにStripeの新着があったかどうかを一切確認できていない（「新着なし」と「未確認」を区別してchangelogに記録した）
- **再現性**: 継続的（許可ドメインへの `stripe.dev` 追加が引き続き未対応）

### 3. パターン1c「設定ファイル・APIに直接関わる検証」と「運用Tips・体験記」の境界判定が本日3件で分かれ目になった
- **種類**: 採用基準
- **発生箇所**: Step 9 採用判定
- **詳細**: 本日却下した4件のうち2件（PageSpeed Insightsスコアのばらつき、GitHub Actions no-opゲート体験談）は、実際のコード例・実際の公式ツールAPI出力フィールド名を使っていたが、内容の主眼が「運用上のTips・教訓」であったため対象外と判定した。一方採用した3件（pnpm minimumReleaseAge、Keychain保存、window undefined）は「公式ツールの設定ファイル形式・エラーコード・API挙動そのもの」が主眼だったため採用した。この線引きは今回は一貫して判断できたが、「実際のAPI出力フィールドを使っている」＝1c該当、と誤読すると採用基準が緩みすぎる余地があり、プロンプト上に明記された基準（「対象は公式ツールの設定ファイル形式・APIに直接関わる検証に限定する。設計論・運用Tips・体験記は対象外」）を毎回慎重に再読する必要があった
- **影響**: 実害はない（今回は正しく却下できた）が、セッションによって判定がぶれるリスクがある
- **再現性**: 単発（今回初めて明確に4件中2件で境界判定が必要になった）

## プロンプト改善提案（1件）

### 1. パターン1c判定に「除外の具体例」を追加する
- **対象セクション**: 「パターン1c: 公式機能の検証記事（限定的に単独採用可）」
- **現状の記述**: 「対象は「公式ツールの設定ファイル形式・API に直接関わる検証」に限定する。設計論・運用 Tips・体験記は対象外」
- **提案する変更**: この一文の直後に、境界の具体例を1〜2個追加する。例:「◯（対象内）: 公式ツールの実際のconfigキー・エラーコード・CLIフラグを示す検証。✕（対象外）: 公式ツールの実際のAPI出力フィールドを使っていても、結論が「運用上こうすべき」という一般的Tipsに留まるもの（例: 計測回数を増やして中央値を取る、fail-loudな設計にする）」
- **理由**: 本日、実際のAPI/JSON出力を使っている記事が2件あり、コード例の有無だけでは1c該当性を判断できず、「主眼が設定/APIの検証か、運用上の教訓か」という追加の軸で判定する必要があった。具体例があれば次回セッションでも同じ基準で判定しやすくなる
- **想定リスク**: なし（既存の却下判断を明文化するだけ）
- **優先度**: 低

## 取り上げなかったが気になる挙動

- Linear フィードで、2026-08-18に一度却下済みの記事（"Rebuilding Linear's delta sync read path"）が本日も直近36h窓に該当して再出現した。`_seen.json` チェックで正しくスキップでき、想定通りの動作だった（ポジティブな確認のため問題としては記録しない）
