# Routine Feedback 2026-05-29

## 実行サマリ
- 採用: 1件 / 強化: 4件
- 採用前チェックでスキップ: 0件
- 取得失敗ソース: 3件（Turborepo permanently_skipped、GitHub Blog Engineering/Next.js Blog 新着なし）
- 気づきレベル: 中

## 観察した問題（2件）

### 1. dev.to typescript タグ: 同一ユーザー大量投稿（`therizwansaleem`）
- **種類**: ソース品質 / 採用基準
- **発生箇所**: Step 6 / dev.to/t/typescript フィード
- **詳細**: `therizwansaleem` ユーザーが36h以内に12件のTypeScript関連記事を投稿。タイトルが似ており（"TypeScript and frontend forms", "TypeScript and frontend testing" など）、コピー/焼き直しの疑いが強い。プロンプトの「同一ユーザー3件以上で除外」ルールを適用した。
- **影響**: 12件を採用判定から除外（D: read_skipped）。うち仮に1件を読んでいたとしても Pattern 2 には使えない。
- **再現性**: 継続的（2026-05-23の `sovereignty-advocate` 事例と同様のパターン）

### 2. `_seen.json` スキーマ不一致
- **種類**: プロンプト記述 / ツール挙動
- **発生箇所**: Step 13（_seen.json 更新）
- **詳細**: プロンプトは新スキーマ（`{version, entries: [{url, fetched_at, status}]}`）を規定しているが、実ファイルはフラット配列（`["url1", "url2", ...]`）のまま。2026-05-06以降の全実行で旧フォーマットを使い続けている。URL の append-only 削除禁止ルールは守れているが、`adopted` / `read_skipped` / `fetch_failed` の区別が記録できていない。
- **影響**: changelog の参照記事一覧で判定を記録しているため運用上の支障は軽微。ただしプロンプトの「`fetch_failed` は24時間後に再試行可能」ルールが機能していない。
- **再現性**: 継続的（毎回発生）

## プロンプト改善提案（1件）

### 1. `_seen.json` 移行手順の明記
- **対象セクション**: `_seen.json の仕様 > スキーマ`
- **現状の記述**: 新スキーマ（`entries: [{url, fetched_at, status}]`）を規定しているが、実ファイルは旧フォーマット（フラット配列）のまま
- **提案する変更**: スキーマセクションの先頭に次を追記:
  ```
  > **移行注記（2026-05現在）**: 実ファイルは互換性維持のためフラット配列フォーマット
  > `["url1", "url2", ...]` のまま運用している。新スキーマへの移行は別タスクで実施予定。
  > このRoutineでは append-only でURLを追記するのみとし、statusフィールドは changelog に記録する。
  ```
- **理由**: プロンプトと実態のギャップにより毎回処理分岐が発生し、Routine 実装が複雑化している
- **想定リスク**: 移行タイミングで URL 削除リスクが発生するため、移行時は4段階 dedupe 検証を厳格に実施する必要がある
- **優先度**: 低

## 取り上げなかったが気になる挙動

- TypeScript 6 の `strict: true` デフォルト化は Jake Bailey の TSKaigi 2026 基調講演の要約記事からのみ確認。Microsoft 公式 TypeScript Blog や GitHub Releases での一次確認ができなかった（GitHub API が 403 を返したため）。確信度「中」として記録したが、将来的に公式ソースで確認次第「高」に更新したい。
- Findy の記事（`AI-DLCをClaude Skill化`）は Claude Code Skills の実践事例として興味深かったが、Rule #11（Skills 運用）に強化するほどの新規性がなかった。次回 Findy の記事で Claude Skills 実践がまた出たら Pattern 2 として採用できる。
