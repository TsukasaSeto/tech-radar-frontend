# Routine Feedback 2026-09-20

## 実行サマリ
- 採用: 0件 / 強化: 0件
- 参照記事数: 38件（うち本文精読3件、タイトルスクリーニングのみ35件）
- 採用前チェックでスキップ: 0件
- 取得失敗ソース: 1件（Stripe Engineering、全段階失敗）
- 気づきレベル: 重大（`main` の長期停滞を新規に検出）

## 既出問題の再確認（直近7日 grep + 手動確認）

`grep -rl "stripe.dev\|EGRESS_BLOCKED\|24ソース\|_seen.json.*スキーマ" feedback/*.md` および `mcp__github__list_pull_requests` で直近の状況を確認した。

### 1. Stripe Engineering の `stripe.dev` EGRESS_BLOCKED（2026-09-07, 2026-09-09 で既出 → 今回で3回連続再現）
`feedback/2026-09-07.md` と `feedback/2026-09-09.md` に同一事象（`stripe.com/blog/engineering` → 301 → `stripe.dev/blog/topic/engineering` がプロキシの許可ドメイン外で `EGRESS_BLOCKED`）が記録されている。今回のセッションでも段階5で全く同じリダイレクト・同じブロックを再現し、フォールバックの段階3（AllOrigins raw）は408タイムアウト、段階4（AllOrigins get）は520エラーで、結局5段階すべて失敗した。**3回連続で同一パターン**であり、単発の障害ではなく恒常的な制約と確定してよい水準。2026-09-07 feedback の改善提案2（実行環境の事前検証ステップ追加）は、少なくとも本セッションが受け取ったプロンプト本文には未反映だった。
- **提案する変更（再掲・具体化）**: `.memo/env-tech-radar-curator.md` の許可ドメインに `stripe.dev` を追加するか、Stripe Engineering をソース一覧から「既知の継続的失敗」として `stripe.com/blog/engineering` の代わりに `stripe.dev/blog/topic/engineering` を直接叩く形に更新する（302先を直接ソースURLにすれば段階5の1ホップで済み、プロキシのリダイレクト追跡が発生しなくなる可能性がある）。
- **優先度**: 高（3回目の再現、対応コストは低いと思われる）

### 2. `_seen.json` のスキーマ乖離（2026-09-07で既出 → 今回も未解消、実データは相変わらずフラット配列）
`feedback/2026-09-07.md` の指摘通り、実データは今回確認時点でも `{version, entries:[...]}` ではなく単純な URL 文字列配列（4184件、`status` フィールドなし）のまま。プロンプト本文の「スキーマ」セクションは2週間以上前から実データと一致していないが、依然として未更新。今回も従来通り「削除禁止ルール」セクションのjqコマンド（配列前提）に従って処理した。
- **提案する変更（再掲）**: プロンプトのスキーマ定義をフラット配列に合わせて修正する。`status` 区別が要る場合は移行コストの高い別Routineでの対応検討が必要（現状は`fetch_failed`の24h再試行判定なども実運用では機能していない）。
- **優先度**: 中（実害は「削除禁止ルール」セクションの記述で吸収されているため緊急ではないが、2週間放置は長い）

### 3. 「24ソース」表記と実フィード数の乖離（2026-09-07で既出 → 今回も未解消）
`feedback/2026-09-07.md` の指摘通り、コミュニティ系4サイトはtopic/tag展開で実際には56フィード（Zenn15+Qiita14+dev.to15+Medium12）、公式・国内・海外・Stripeを含めると75以上になる。今回のセッションでは時間制約により、関心領域上の優先度が高い10フィードのみ実査し、残り46フィードは未着手のまま「参照した記事一覧」に含めていない。2026-09-07 feedbackの改善提案1（優先順位と1日あたりの目安件数の明文化）はまだ反映されていない。
- **優先度**: 高（3回目の指摘。優先順位が実行者裁量のままだと、日によって手薄になるトピックが変わり続ける）

## 新たに観察した重大な問題

### 4. `main` ブランチが2026-09-09で停止し、`practice-update` / `seen-json-update` の未マージPRが16本（#240〜#255、2026-09-10〜2026-09-19分）滞留している
- **種類**: リポジトリ運用（routine-followup側の停止）
- **詳細**: `mcp__github__list_pull_requests`（state=open）で確認したところ、2026-09-10から2026-09-19まで、ほぼ毎日2本ずつ（`practice-update/YYYY-MM-DD` + `seen-json-update/YYYY-MM-DD`）、計16本のPRがopenのまま滞留していた。`main` の最新コミットは2026-09-09付。CLAUDE.mdが想定する「毎朝Routineが起票 → ローカルセッションが取り込み」のサイクルのうち、取り込み側（routine-followup）が10日間動いていないことになる。
- **今回の実行への影響**:
  - Step 9のルール採番・Step 2のmain SHA取得は仕様通り`origin/main`基準で行った（今回は新規ルール0件のため番号衝突は発生しない）が、次回以降にA判定が出た場合、`main`基準の採番は未マージ16PR分の変更を反映しておらず、後日それらがマージされる際に番号衝突するリスクが高い（CLAUDE.mdが名指しする`practices/ai-agent/claude-code.md`は特に要注意）。
  - `_seen.json`の重複排除も`origin/main`（4184件）基準で行ったが、未マージの8本の`seen-json-update`PR（#241,#243,#245,#247,#249,#251,#253,#255）はそれぞれ独自にURLを追加済み。念のため直近1本（`seen-json-update/2026-09-19`、+10件）との重複を個別に確認したところ、今回の38候補との重複はゼロだった（詳細はchangelog参照）。ただし残り7本は未突合であり、将来のマージ時にコンフリクトまたは重複追記が発生する可能性がある。
- **提案する変更**: 至急 `routine-followup` セッションを実行し、#240〜#255（特に古い方から）を安全な順にマージする必要がある。マージが10日以上遅延すると、未マージPR同士の`_seen.json`コンフリクト解消コストが積み上がる一方。
- **優先度**: 重大・緊急

### 5. dev.to `ai` タグの個別記事取得結果が自己矛盾していた（ツール挙動）
- **種類**: ツール挙動（WebFetchの要約が不安定）
- **詳細**: `https://dev.to/feed/tag/ai` の取得結果で、要約が冒頭「NONE IN WINDOW」と明言しつつ、直後に「全記事が窓内（20:41〜21:04 UTC）」と矛盾する説明を返し、記事を1件だけ提示した。RSS本体を直接検証していないため、実際に窓内記事が1件だけなのか複数あるのか確定できていない。今回は保守的に1件のみ候補として扱い、内容が関心領域外（AIバイアス監査論）だったため実害はなかったが、次回以降に有望な候補がこのパターンで出た場合は再フェッチでの確認が必要。
- **優先度**: 低（実害なし、観察記録のみ）

### 6. Zenn の複数トピック（typescript/aiagent/claudecode）で「Jev」という架空プロダクトを扱う記事が同日に多数（typescript topicだけで2件、aiagent/claudecodeで各2〜3件）横断的に出現
- **種類**: ソース品質
- **詳細**: 異なる著者名（marvelousu, petitbouquet, karaage0703, chobitnet 等）が同日に「Jev」という共通の(架空と思われる)プロダクト名を含む記事を複数トピックにまたがって投稿していた。内容はゲームAI、確率分布、記憶想起など多岐にわたり、SEO目的の量産コンテンツ、あるいはベンチマーク/テスト用に注入されたダミー記事である可能性がある。フロントエンド関心領域外と判断しすべて除外したが、今後Zennの`aiagent`/`claudecode`トピックでこの傾向が続く場合は要注意。
- **優先度**: 低〜中（観察記録、今回の判定への影響なし）

### 7. 個別記事の本文取得（段階A）で、RSS上の公開日時と本文ページから抽出した公開日時が食い違うケースを2件観測
- **種類**: ツール挙動（WebFetchの日付抽出）
- **詳細**: `dev.to/jsmanifest/...`と`dev.to/sameer_hassan/...`の2記事で、RSS一覧（段階1）ではpubDateが2026-09-20だったが、個別記事ページ（段階A）の要約では「published September 20, **2024**」と2年古い日付が返された。dev.toのプロフィール/バイト欄にある無関係な日付をAIが誤読した可能性が高い。今回はRSSのpubDateを正として36h窓内判定を継続したが、公開日時の実在性そのものに疑義が生じたため、確信度評価を「個人ブログ単独・内容も既存ルールと重複」という理由で最終的にD判定にした一因にもなった。
- **提案する変更**: 個別記事のプロンプトに「ページ内に複数の日付表記がある場合、RSSのpubDateを優先し、本文中の日付は補助情報として扱う」旨を明記する。
- **優先度**: 低〜中

## 採用基準の判断で迷った点

- `https://zenn.dev/tamatamatama/articles/749173d643e6bd`（プリレンダー済みSPAが検索170位だった件）は、領域としては新規（`web-standards`/`architecture`のいずれにも未収載）で、パターン2不成立でも「確信度中」の単独記事として救えないか検討したが、著者自身が「順位回復は未確認」と明言している時点で結果の再現性が担保されておらず、確信度「低」→新規不採用のルールに従いDとした。**次回以降、同テーマ（クライアントレンダリングのみのSPAでクロール可能なリンクが存在しない場合のSEO低下）の2本目の記事が見つかれば、B（新規ルール）候補として引き上げるべき**。今回のD判定は「言い訳」ではなく判定基準への厳密な適用の結果である。

## 取り上げなかったが気になる挙動

- Medium `nextjs` タグは今回も個人ブログ（`@`始まりの著者）が大半で、既存ルールの反復的な解説記事（キャッシュ、画像最適化、レンダリング手法）が多かった。プロンプトの「単一記事のみで採用するのは避ける」という注意書き（現状 dev.to `webdev`/`ai` 限定）を Medium `nextjs`/`frontend` にも拡張する価値がありそうだが、今回の判定自体はいずれにせよ既存ルール重複でD/対象外だったため実害はなかった。
